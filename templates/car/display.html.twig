{% extends 'base.html.twig' %}

{% block title %}Hello CarController!{% endblock %}

{% block body %}

<h1>{{car.modele}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th scope="row">ID</th>
                <td>{{car.id}}</td>
            </tr>
            <tr>
                <th scope="row">Nom</th>
                <td>{{car.modele}}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
